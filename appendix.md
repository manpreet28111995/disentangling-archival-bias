# Experiment Runtime Appendix

`run_experiment.py` writes a run-specific copy under the output directory after
each complete real-data run. That generated copy contains actual timestamp, Git
commit, software/device, query, cohort, model, prompt, and artifact details.

Use `results_full_run/appendix.md` when merging runtime details into manuscript
Methods or supplementary material.
